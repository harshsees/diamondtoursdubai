# Where contact-form submissions are delivered.
# Any endpoint that accepts a JSON POST works: Zapier, Make, a Slack incoming
# webhook, or your own CRM. Leave unset in development — submissions are then
# logged to the server console and the form still succeeds.
CONTACT_WEBHOOK_URL=
